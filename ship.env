# ship.sh configuration — see factory/ship/README.md
APP_ID=6799616622
SCHEME=Nightwatch
TEAM_ID=N462N35929

# Optional. Sensible defaults are used when these are left unset.
# PROJECT=Nightwatch.xcodeproj
# PROJECT_YML=project.yml      # omit for a non-XcodeGen project
# INFO_PLIST=                  # required only when PROJECT_YML is absent
# CONFIGURATION=Release
# VERSION_ID=                  # ASC version uuid, for 'ship.sh attach'
# ASC_KEY_ID=                  # defaults to asc's default credential
# ASC_KEY_PATH=                # defaults to the usual AuthKey_<id>.p8 locations
# ASC_ISSUER_ID=               # defaults to 'asc auth issuer-id'
